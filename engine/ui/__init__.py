"""UI runtime package — see ``docs/components/08-ui-runtime.md``.

Owns ``ui_runtime.py`` (the widget-tree parser/panel-stack runtime) and
``targeting_overlay.py`` (the engine-owned spell-targeting cursor, a
distinct thing from a panel — see that doc §1/§2.4).
"""
